import * as React from 'react';
import {Text, SafeAreaView} from 'react-native';

const NeedOneScreen = ({navigation}) => {
  return (
    <SafeAreaView>
      <Text style={{textAlign:'center', marginTop:200, fontSize:24}}> Need One Feature Coming Soon! </Text>
    </SafeAreaView>
  );
};
export default NeedOneScreen;
